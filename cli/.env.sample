# Copy to .env and set your credentials
# These are only used to pass values into CLI flags.

NITTYMAIL_IMAP_ADDRESS=example@gmail.com
NITTYMAIL_IMAP_PASSWORD=app-password-xxxx-xxxx

# Redis configuration for job mode
REDIS_URL=redis://redis:6379/0

# Sidekiq Web (monitor) optional auth
# SIDEKIQ_WEB_USER=admin
# SIDEKIQ_WEB_PASSWORD=secret
# SIDEKIQ_SESSION_SECRET=change-me

# Worker scaling (used by ./bin/up-workers)
# Number of Sidekiq process replicas to run for each role
FETCH_PROCESSES=8
WRITE_PROCESSES=1
